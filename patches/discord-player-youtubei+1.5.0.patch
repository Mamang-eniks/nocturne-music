diff --git a/node_modules/discord-player-youtubei/dist/index.js b/node_modules/discord-player-youtubei/dist/index.js
index 3f415d3..d19e193 100644
--- a/node_modules/discord-player-youtubei/dist/index.js
+++ b/node_modules/discord-player-youtubei/dist/index.js
@@ -156,7 +156,7 @@ async function streamFromYT(query, innerTube, options = {
       videoInfo
     );
   } else {
-    format.url = format.decipher(innerTube.session.player);
+    format.url = await format.decipher(innerTube.session.player);
     if (!format.content_length)
       throw new Error("Not matching URL for this format found");
     return createNativeReadable(
@@ -726,7 +726,7 @@ ${error}`);
       quality: "best",
       format: "mp4"
     });
-    format.url = format.decipher(this.innerTube.session.player);
+    format.url = await format.decipher(this.innerTube.session.player);
     return createNativeReadable(
       format.url,
       format.content_length,
diff --git a/node_modules/discord-player-youtubei/dist/index.mjs b/node_modules/discord-player-youtubei/dist/index.mjs
index 499a1cc..91fda2f 100644
--- a/node_modules/discord-player-youtubei/dist/index.mjs
+++ b/node_modules/discord-player-youtubei/dist/index.mjs
@@ -115,7 +115,7 @@ async function streamFromYT(query, innerTube, options = {
       videoInfo
     );
   } else {
-    format.url = format.decipher(innerTube.session.player);
+    format.url = await format.decipher(innerTube.session.player);
     if (!format.content_length)
       throw new Error("Not matching URL for this format found");
     return createNativeReadable(
@@ -685,7 +685,7 @@ ${error}`);
       quality: "best",
       format: "mp4"
     });
-    format.url = format.decipher(this.innerTube.session.player);
+    format.url = await format.decipher(this.innerTube.session.player);
     return createNativeReadable(
       format.url,
       format.content_length,
